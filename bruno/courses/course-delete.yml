info:
  name: course-delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{courses}}/2"
  auth:
    type: bearer
    token: "{{instructor_access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
