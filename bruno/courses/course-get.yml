info:
  name: course-get
  type: http
  seq: 3

http:
  method: GET
  url: "{{courses}}/1"
  auth:
    type: bearer
    token: "{{admin_access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
